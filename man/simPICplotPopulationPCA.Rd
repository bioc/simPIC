% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simPICpopulationPlots.R
\name{simPICplotPopulationPCA}
\alias{simPICplotPopulationPCA}
\title{Plot Population-Style PCA Summaries for Real Data}
\usage{
simPICplotPopulationPCA(
  counts,
  sample.col = NULL,
  batch.col = NULL,
  sample.pattern = "#.*$",
  sample.replacement = "",
  pca.ntop = 2000,
  pca.components = 10,
  aggregate.by.sample = TRUE,
  point.size = 0.8,
  verbose = TRUE
)
}
\arguments{
\item{counts}{A peak-by-cell count matrix, sparse \code{Matrix}, or
\code{SingleCellExperiment}.}

\item{sample.col}{Optional name of a sample column already present in
\code{colData(counts)}. If \code{NULL}, sample IDs are inferred from column
names using \code{sample.pattern}.}

\item{batch.col}{Optional name of a batch column already present in
\code{colData(counts)}.}

\item{sample.pattern}{Regular expression used to strip the barcode suffix
from cell names when inferring sample IDs.}

\item{sample.replacement}{Replacement string used with
\code{sample.pattern}.}

\item{pca.ntop}{Number of most variable peaks to use for PCA.}

\item{pca.components}{Number of principal components to compute.}

\item{aggregate.by.sample}{Logical. Whether to also aggregate cells by sample
and create a donor-level PCA plot.}

\item{point.size}{Point size passed to \code{scater::plotPCA()}.}

\item{verbose}{Logical. Whether to print progress messages.}
}
\value{
A list containing:
\describe{
\item{\code{cell_sce}}{Cell-level \code{SingleCellExperiment} with inferred
metadata, log-normalized counts, and PCA.}
\item{\code{sample_sce}}{Sample-aggregated \code{SingleCellExperiment} if
\code{aggregate.by.sample = TRUE}, otherwise \code{NULL}.}
\item{\code{plots}}{A named list of \code{ggplot} objects.}
}
}
\description{
Create splatPop-style PCA plots from a real peak-by-cell matrix or
\code{SingleCellExperiment}. When metadata is not already available, sample
IDs can be inferred from cell names, which is useful for peak-by-cell
matrices where columns are encoded like \code{sample#barcode}.
}
\examples{
if (requireNamespace("scater", quietly = TRUE)) {
    counts <- matrix(rpois(50 * 24, lambda = 3), nrow = 50, ncol = 24)
    colnames(counts) <- paste0(
        rep(paste0("Sample", 1:6), each = 4),
        "#Cell",
        seq_len(ncol(counts))
    )
    plots <- simPICplotPopulationPCA(counts, verbose = FALSE)
    names(plots$plots)
}
}
