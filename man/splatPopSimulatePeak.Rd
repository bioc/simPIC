% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simPICsplatPop.R
\name{splatPopSimulatePeak}
\alias{splatPopSimulatePeak}
\title{Simulate Peak-Based Population Data with splatPop}
\usage{
splatPopSimulatePeak(
  params = NULL,
  vcf = NULL,
  method = c("single", "groups", "paths"),
  gff = NULL,
  eqtl = NULL,
  means = NULL,
  key = NULL,
  counts.only = FALSE,
  sparsify = TRUE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{params}{Optional \code{SplatPopParams} object. If \code{NULL}, a new
object is created.}

\item{vcf}{Optional \code{VariantAnnotation} VCF object. If \code{NULL}, the
\code{splatter} mock VCF is used.}

\item{method}{Simulation mode passed through to \code{splatPopSimulate()}.}

\item{gff}{Peak annotation supplied either as \code{NULL}, a GFF-like
\code{data.frame}, or a \code{GRanges}. Peak annotations are coerced to a
GFF-like table and stored as gene-like features for the underlying splatPop
machinery.}

\item{eqtl}{Optional empirical eQTL table.}

\item{means}{Optional empirical population means matrix.}

\item{key}{Optional splatPop key.}

\item{counts.only}{Logical. Whether to keep only counts in the simulated
object.}

\item{sparsify}{Logical. Whether to sparsify the resulting assays.}

\item{verbose}{Logical. Whether to print progress messages.}

\item{...}{Additional parameters passed to \code{splatter::splatPopSimulate}.}
}
\value{
A \code{SingleCellExperiment} object.
}
\description{
Simulate peak-based single-cell data using \code{splatter::splatPopSimulate}
while coercing peak annotations into the GFF-like structure that splatPop
expects internally.
}
\examples{
if (requireNamespace("splatter", quietly = TRUE) &&
    requireNamespace("VariantAnnotation", quietly = TRUE)) {
    params <- splatter::newSplatPopParams(nGenes = 20)
    params <- splatter::setParams(params, batchCells = c(20))
    sim <- splatPopSimulatePeak(
        params = params,
        vcf = splatter::mockVCF(),
        gff = splatter::mockGFF()[seq_len(20), ],
        sparsify = FALSE,
        verbose = FALSE
    )
    sim
}
}
